{
  "name": "@effect-ui/react",
  "version": "0.0.0-alpha.0",
  "description": "React runtime provider, router, resource, action, program, stream, and scope bindings for Effect UI.",
  "license": "MIT",
  "author": "Andrew Lee",
  "files": [
    "dist"
  ],
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist .tsbuildinfo && tsgo -p tsconfig.json",
    "typecheck": "tsgo -p tsconfig.json --pretty false --noEmit"
  },
  "dependencies": {
    "@effect-ui/core": "workspace:*",
    "effect": "^4.0.0-beta.66"
  },
  "peerDependencies": {
    "react": ">=18.3.0 <21"
  }
}
